---
name: Steam Deck report
about: The game on a Steam Deck (Linux build or Windows build under Proton)
title: "[Deck] "
labels: steam-deck
---

<!-- docs/steam-deck-testing.md says what to run and where the files are. -->

**Which build:** Linux AppImage / Linux tar.zst / Windows zip under Proton (version: )
**Release version:** v1.0.
**SteamOS version (Settings → System):**
**Desktop Mode or Game Mode:**

**What you saw** (one of: closed / closed after the launcher / closed after the
progress bar / black window / slow / plays — plus how long after launch, and the fps
from the window title if it got that far):

**Attach:**
- [ ] `cz_diag.txt` (from `--diag`) or its output pasted below
- [ ] `cz_runtime.log` (beside the game's `assets/` folder, or beside the `.AppImage`)
- [ ] for Proton: `~/steam-<appid>.log` with `PROTON_LOG=1 %command%` set

```
paste --diag output here
```
